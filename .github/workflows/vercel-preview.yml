# This workflow was removed. Vercel GitHub App handles preview deployments.
name: (Removed) Vercel Preview

on:
  # Manual only; does not run on PRs or pushes
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel Preview workflow removed; use Vercel GitHub App deployments."
